/**
 * SERVICE: CAPACITOR (CAMERA & LOCATION)
 * Purpose: Centralize access to native features.
 * 
 * Logic Flow:
 * 1. Request permissions for Camera and Geolocation.
 * 2. Define `captureIssueImage` using Camera.getPhoto (Base64).
 * 3. Define `getCurrentLocation` using Geolocation.getCurrentPosition.
 * 
 * Returns: { imageUrl/base64, coords: { lat, lng } }
 */

// export const captureIssueImage = async () => { ... };
// export const getIssueCoords = async () => { ... };
