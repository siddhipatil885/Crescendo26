/**
 * CONSTANTS: CIVIX APP
 * Purpose: Centralized configuration for categories and status types.
 * 
 * Logic Flow:
 * 1. Define `ISSUE_CATEGORIES` (e.g., Pothole, Waste, Infrastructure).
 * 2. Define `ISSUE_STATUS` (e.g., Pending, In-Progress, Resolved).
 * 3. Define `UI_COLORS` (CSS color codes for mapping status badges).
 */

// export const CATEGORIES = ['Pothole', 'Waste', ...];
// export const STATUS = { PENDING: 'pending', ... };
// export const COLORS = { [STATUS.PENDING]: 'red', ... };
