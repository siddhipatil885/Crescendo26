/**
 * COMPONENT: ISSUE CARD
 * Props: { issue }
 * UI: Thumbnail (before), AI Category, Snippet of Description, Status Badge.
 * Logic: Navigate to details page on click.
 */

// export const IssueCard = () => { ... };

/**
 * COMPONENT: STATUS BADGE
 * Props: { status }
 * UI: Small pill-shaped tag.
 * Logic: Map status (pending, active, resolved) to Theme colors (red, orange, green).
 */

// export const StatusBadge = () => { ... };

/**
 * COMPONENT: BEFORE/AFTER VIEW
 * Props: { beforeUrl, afterUrl }
 * UI: Side-by-side or slider comparison.
 * Logic: Handles image loading states and fallback placeholders.
 */

// export const BeforeAfterView = () => { ... };
