/**
 * COMPONENT: IMAGE UPLOADER
 * Props: { onPhotoCapture(base64), onUrlUpload(url) }
 * UI: Camera Icon/Button and Preview area with status 'Success/Error'.
 * Logic: Bridge to `capacitor.js` camera and `storage.js` for uploads.
 */

// export const ImageUploader = () => { ... };

/**
 * COMPONENT: MAP VIEW
 * Props: { issues, centerCoords, zoom }
 * UI: Map container (Leaflet/Google Maps).
 * Logic: Plot pins for each issue. Link pin click to `IssueDetails` route.
 */

// export const MapView = () => { ... };
